// AUTO-GENERATED — do not edit. Source: same filename with .jsx extension.
// Generated by scripts/build-jsx.js using @babel/preset-react.
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// ================================================================
// HESTÍA — Disponibilidad + Selección de fechas
// Lee docs/assets/availability.json (generado por GitHub Action)
// ================================================================

const _CM = {
  months_es: ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'],
  months_en: ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'],
  wd_es: ['Lu', 'Ma', 'Mi', 'Ju', 'Vi', 'Sá', 'Do'],
  wd_en: ['Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa', 'Su'],
  apt_names: {
    vm: 'Hestía Mar',
    vt: 'Hestía Thalassa',
    vs: 'Hestía Salinas'
  }
};

// --- Date helpers ---
const _adj = (ds, n) => {
  const d = new Date(ds + 'T12:00:00Z');
  d.setUTCDate(d.getUTCDate() + n);
  return d.toISOString().slice(0, 10);
};
const _isBlk = (ds, blocked) => blocked.some(r => ds >= r.start && ds < r.end);
const _diff = (a, b) => Math.round((new Date(b + 'T12:00:00Z') - new Date(a + 'T12:00:00Z')) / 86400000);
const _maxConsec = (startDs, blocked) => {
  let n = 0,
    cur = startDs;
  while (!_isBlk(cur, blocked) && n <= 366) {
    n++;
    cur = _adj(cur, 1);
  }
  return n;
};
const _hexA = (hex, a) => (hex || '#3aaabb') + Math.round(a * 255).toString(16).padStart(2, '0');
const _longDate = (ds, lang) => {
  if (!ds) return '';
  const d = new Date(ds + 'T12:00:00Z');
  const M = (lang === 'es' ? _CM.months_es : _CM.months_en)[d.getUTCMonth()];
  const W = lang === 'es' ? ['Dom', 'Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb'][d.getUTCDay()] : ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'][d.getUTCDay()];
  return `${W} ${d.getUTCDate()} ${M.slice(0, 3).toLowerCase()}. ${d.getUTCFullYear()}`;
};
const _shortDate = (ds, lang) => {
  if (!ds) return '';
  const d = new Date(ds + 'T12:00:00Z');
  const M = (lang === 'es' ? _CM.months_es : _CM.months_en)[d.getUTCMonth()];
  return `${d.getUTCDate()} ${M.slice(0, 3).toLowerCase()}.`;
};

// ---------------------------------------------------------------
// RequestPanel — aparece cuando hay fechas seleccionadas
// ---------------------------------------------------------------
const RequestPanel = ({
  aptId,
  lang,
  accent,
  selStart,
  selEnd,
  onReset
}) => {
  const [guests, setGuests] = React.useState(2);
  // Pet y baby SÍ se eligen aquí porque afectan al precio (mascota) y
  // a la preparación (bebé). El resto de extras (cuna, trona, sábanas
  // adicionales, toallas adicionales) se eligen en /reservas.
  const [pets, setPets] = React.useState(false);
  const [baby, setBaby] = React.useState(false);
  const aptName = _CM.apt_names[aptId] || 'Hestía';
  const calc = _calcStay(selStart, selEnd, aptId, pets);
  const fmt = n => n.toLocaleString('es-ES') + ' €';
  const months_es = ['enero', 'febrero', 'marzo', 'abril', 'mayo', 'junio', 'julio', 'agosto', 'septiembre', 'octubre', 'noviembre', 'diciembre'];
  const months_en = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
  const fmtDate = ds => {
    if (!ds) return '';
    const d = new Date(ds + 'T12:00:00Z');
    if (lang === 'es') return `${d.getUTCDate()} de ${months_es[d.getUTCMonth()]} de ${d.getUTCFullYear()}`;
    return `${months_en[d.getUTCMonth()]} ${d.getUTCDate()}, ${d.getUTCFullYear()}`;
  };

  // CTA "Avanzar con la reserva": navega a /reservas pre-rellenado.
  // Datos de contacto, extras detallados (cuna, trona, sábanas, toallas)
  // y comentarios se gestionan allí — aquí solo lo esencial.
  const reservasHref = (() => {
    const params = new URLSearchParams();
    params.set('apt', aptId);
    if (selStart) params.set('checkin', selStart);
    if (selEnd) params.set('checkout', selEnd);
    if (guests) params.set('guests', String(guests));
    if (pets) params.set('pets', 'yes');
    if (baby) params.set('baby', 'yes');
    return 'reservas.html?' + params.toString();
  })();
  return /*#__PURE__*/React.createElement("div", {
    className: "req-panel",
    style: {
      '--req-accent': accent
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "req-dates"
  }, /*#__PURE__*/React.createElement("div", {
    className: "req-date-col"
  }, /*#__PURE__*/React.createElement("span", {
    className: "req-date-lbl"
  }, lang === 'es' ? 'Entrada' : 'Check-in'), /*#__PURE__*/React.createElement("span", {
    className: "req-date-val"
  }, fmtDate(selStart))), /*#__PURE__*/React.createElement("div", {
    className: "req-nights"
  }, /*#__PURE__*/React.createElement("span", {
    className: "req-nights-n"
  }, calc ? calc.nights : '—'), /*#__PURE__*/React.createElement("span", {
    className: "req-nights-lbl"
  }, lang === 'es' ? 'noches' : 'nights')), /*#__PURE__*/React.createElement("div", {
    className: "req-date-col req-date-col--r"
  }, /*#__PURE__*/React.createElement("span", {
    className: "req-date-lbl"
  }, lang === 'es' ? 'Salida' : 'Check-out'), /*#__PURE__*/React.createElement("span", {
    className: "req-date-val"
  }, fmtDate(selEnd)))), calc && /*#__PURE__*/React.createElement("div", {
    className: "price-engine"
  }, /*#__PURE__*/React.createElement("div", {
    className: "price-main-row"
  }, /*#__PURE__*/React.createElement("div", {
    className: "price-direct-block"
  }, /*#__PURE__*/React.createElement("span", {
    className: "price-label-sm"
  }, lang === 'es' ? 'Precio directo · hasta' : 'Direct price · up to'), /*#__PURE__*/React.createElement("span", {
    className: "price-direct-total"
  }, fmt(calc.directTotal)), /*#__PURE__*/React.createElement("span", {
    className: "price-avg-night"
  }, fmt(calc.avgPerNight), lang === 'es' ? '/noche' : '/night')), /*#__PURE__*/React.createElement("div", {
    className: "price-right-col"
  }, /*#__PURE__*/React.createElement("div", {
    className: "price-guarantee-badge"
  }, lang === 'es' ? '✓ Mejor precio garantizado' : '✓ Best price guarantee'), /*#__PURE__*/React.createElement("div", {
    className: "price-guarantee-sub"
  }, lang === 'es' ? 'Si encuentras un precio mejor, te lo mejoramos.' : 'See a better price elsewhere? We\'ll beat it.'))), /*#__PURE__*/React.createElement("div", {
    className: "price-breakdown"
  }, /*#__PURE__*/React.createElement("div", {
    className: "price-line"
  }, /*#__PURE__*/React.createElement("span", null, lang === 'es' ? `${calc.nights} noches × precio variable` : `${calc.nights} nights × variable rate`), /*#__PURE__*/React.createElement("span", null, fmt(calc.baseTotal))), calc.stayD && /*#__PURE__*/React.createElement("div", {
    className: "price-line price-line-disc"
  }, /*#__PURE__*/React.createElement("span", null, lang === 'es' ? calc.stayD.es : calc.stayD.en), /*#__PURE__*/React.createElement("span", null, "\u2212", fmt(calc.stayDiscAmt))), calc.petAmt > 0 && /*#__PURE__*/React.createElement("div", {
    className: "price-line"
  }, /*#__PURE__*/React.createElement("span", null, lang === 'es' ? 'Suplemento mascota (10 €/noche · máx. 50 €)' : 'Pet supplement (10 €/night · max 50 €)'), /*#__PURE__*/React.createElement("span", null, "+", fmt(calc.petAmt))), /*#__PURE__*/React.createElement("div", {
    className: "price-line price-line-total"
  }, /*#__PURE__*/React.createElement("span", null, lang === 'es' ? 'Precio máximo directo' : 'Maximum direct price'), /*#__PURE__*/React.createElement("span", null, fmt(calc.directTotal)))), /*#__PURE__*/React.createElement("p", {
    className: "price-note"
  }, lang === 'es' ? '* Precio máximo orientativo. Si ves un precio mejor en cualquier plataforma, te lo mejoramos. Cuéntanos de ti y los tuyos — casi siempre podemos ajustar.' : '* Indicative maximum price. If you find a better price anywhere, we\'ll beat it. Tell us about your situation — we can almost always adjust.')), /*#__PURE__*/React.createElement("div", {
    className: "req-guests"
  }, /*#__PURE__*/React.createElement("span", {
    className: "req-guests-lbl"
  }, lang === 'es' ? 'Huéspedes' : 'Guests'), /*#__PURE__*/React.createElement("div", {
    className: "req-guests-ctrl"
  }, /*#__PURE__*/React.createElement("button", {
    className: "req-g-btn",
    onClick: () => setGuests(g => Math.max(1, g - 1)),
    "aria-label": lang === 'es' ? 'Quitar huésped' : 'Remove guest'
  }, "\u2212"), /*#__PURE__*/React.createElement("span", {
    className: "req-g-num"
  }, guests), /*#__PURE__*/React.createElement("button", {
    className: "req-g-btn",
    onClick: () => setGuests(g => Math.min(6, g + 1)),
    "aria-label": lang === 'es' ? 'Añadir huésped' : 'Add guest'
  }, "+")), /*#__PURE__*/React.createElement("label", {
    className: "req-pets-toggle"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: pets,
    onChange: e => setPets(e.target.checked)
  }), /*#__PURE__*/React.createElement("span", null, lang === 'es' ? '🐾 Mascota' : '🐾 Pet')), /*#__PURE__*/React.createElement("label", {
    className: "req-pets-toggle"
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: baby,
    onChange: e => setBaby(e.target.checked)
  }), /*#__PURE__*/React.createElement("span", null, lang === 'es' ? '👶 Bebé' : '👶 Baby'))), /*#__PURE__*/React.createElement("div", {
    className: "req-disclaimer"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "12",
    y1: "8",
    x2: "12",
    y2: "12"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "12",
    y1: "16",
    x2: "12.01",
    y2: "16"
  })), /*#__PURE__*/React.createElement("p", null, lang === 'es' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("strong", null, "Precio m\xE1ximo orientativo."), " Si encuentras un precio m\xE1s bajo te lo mejoramos. En la siguiente pantalla podr\xE1s a\xF1adir extras (cuna, trona, s\xE1banas, mascota\u2026) y dejarnos tus datos. Alex confirma en menos de 24 h.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("strong", null, "Maximum indicative price."), " Find a lower price anywhere \u2014 we will beat it. On the next screen you can add extras (cot, high chair, linen, pet\u2026) and leave your details. Alex confirms within 24 h."))), /*#__PURE__*/React.createElement("div", {
    className: "req-actions req-actions-forward"
  }, /*#__PURE__*/React.createElement("a", {
    href: reservasHref,
    className: "btn btn-primary req-btn-forward"
  }, lang === 'es' ? 'Avanzar con la reserva' : 'Continue with the booking', /*#__PURE__*/React.createElement("span", {
    className: "arrow"
  }, " \u2192"))), /*#__PURE__*/React.createElement("button", {
    className: "req-reset",
    onClick: onReset
  }, "\u2190 ", lang === 'es' ? 'Cambiar fechas' : 'Change dates'));
};

// ---------------------------------------------------------------
// CalMonth — rejilla de un mes
// ---------------------------------------------------------------
const CalMonth = ({
  year,
  month,
  blocked,
  lang,
  todayStr,
  horizonStr,
  selStart,
  selEnd,
  previewEnd,
  onDayClick,
  onDayHover,
  onDayLeave
}) => {
  const wds = lang === 'es' ? _CM.wd_es : _CM.wd_en;
  const mName = (lang === 'es' ? _CM.months_es : _CM.months_en)[month];
  const nDays = new Date(year, month + 1, 0).getDate();
  let firstDow = new Date(year, month, 1).getDay();
  firstDow = firstDow === 0 ? 6 : firstDow - 1;
  const cells = [];
  for (let i = 0; i < firstDow; i++) cells.push({
    empty: true,
    k: `e${i}`
  });
  for (let d = 1; d <= nDays; d++) cells.push({
    d,
    k: d
  });
  return /*#__PURE__*/React.createElement("div", {
    className: "cal-month"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cal-mhd"
  }, mName, " ", /*#__PURE__*/React.createElement("span", {
    className: "cal-yr"
  }, year)), /*#__PURE__*/React.createElement("div", {
    className: "cal-grid"
  }, wds.map(w => /*#__PURE__*/React.createElement("div", {
    key: w,
    className: "cal-wd"
  }, w)), cells.map(cell => {
    if (cell.empty) return /*#__PURE__*/React.createElement("div", {
      key: cell.k,
      className: "cal-cell cal-empty"
    });
    const {
      d
    } = cell;
    const ds = `${year}-${String(month + 1).padStart(2, '0')}-${String(d).padStart(2, '0')}`;
    const isPast = ds < todayStr;
    // Beyond booking horizon = no aceptamos reservas con check-in
    // posterior a esa fecha (ej. cierre antes de Sem. Santa 2027).
    // Se trata visualmente como "past" — gris, no clickable.
    const beyondHorizon = !!(horizonStr && ds > horizonStr);
    const isToday = ds === todayStr;
    const isBlk = _isBlk(ds, blocked);

    // Blocked range markers (only when not in selection)
    const prevBlk = isBlk && _isBlk(_adj(ds, -1), blocked);
    const nextBlk = isBlk && _isBlk(_adj(ds, 1), blocked);
    const isBlkStart = isBlk && !prevBlk;
    const isBlkEnd = isBlk && !nextBlk;
    const isBlkSingle = isBlkStart && isBlkEnd;
    const isBlkMid = isBlk && !isBlkStart && !isBlkEnd;

    // Selection markers
    const inSel = selStart && selEnd && ds >= selStart && ds <= selEnd;
    const isSS = inSel && ds === selStart;
    const isSE = inSel && ds === selEnd;
    const isSM = inSel && !isSS && !isSE;

    // Preview markers (only when selStart set, no selEnd, previewEnd computed)
    const inPrev = !selEnd && selStart && previewEnd && ds >= selStart && ds <= previewEnd;
    const isPS = inPrev && ds === selStart;
    const isPE = inPrev && ds === previewEnd;
    const isPM = inPrev && !isPS && !isPE;
    const isClickable = !isPast && !beyondHorizon && !isBlk;
    const showBlk = isBlk && !inSel && !inPrev;
    return /*#__PURE__*/React.createElement("div", {
      key: d,
      className: ['cal-cell', (isPast || beyondHorizon) && 'past', isToday && 'today', isBlk && 'blk', isClickable && 'clickable', inSel && 'in-sel', isSS && 'sel-s', isSE && 'sel-e', isSM && 'sel-m', inPrev && 'in-prev', isPS && 'prev-s', isPE && 'prev-e', isPM && 'prev-m'].filter(Boolean).join(' '),
      onClick: isClickable ? () => onDayClick(ds) : undefined,
      onMouseEnter: isClickable ? () => onDayHover(ds) : undefined,
      onMouseLeave: isClickable ? onDayLeave : undefined
    }, showBlk && !isBlkSingle && isBlkStart && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-sr"
    }), showBlk && !isBlkSingle && isBlkEnd && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-sl"
    }), showBlk && isBlkMid && /*#__PURE__*/React.createElement("div", {
      className: "c-strip"
    }), showBlk && (isBlkStart || isBlkEnd || isBlkSingle) && /*#__PURE__*/React.createElement("div", {
      className: "c-circ"
    }), isSS && !isSE && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-sel-strip c-sr"
    }), isSE && !isSS && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-sel-strip c-sl"
    }), isSM && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-sel-strip"
    }), (isSS || isSE) && /*#__PURE__*/React.createElement("div", {
      className: "c-circ c-sel-circ"
    }), isPS && !isPE && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-prev-strip c-sr"
    }), isPE && !isPS && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-prev-strip c-sl"
    }), isPM && /*#__PURE__*/React.createElement("div", {
      className: "c-strip c-prev-strip"
    }), (isPS || isPE) && /*#__PURE__*/React.createElement("div", {
      className: "c-circ c-prev-circ"
    }), isToday && !inSel && !inPrev && /*#__PURE__*/React.createElement("div", {
      className: "c-today"
    }), /*#__PURE__*/React.createElement("span", {
      className: "c-n"
    }, d));
  })));
};

// ---------------------------------------------------------------
// AptCalendar — sección principal
// ---------------------------------------------------------------
const AptCalendar = ({
  aptId,
  lang,
  accent
}) => {
  const [data, setData] = React.useState(null);
  const [loading, setLoading] = React.useState(true);
  const now = new Date();
  const todayStr = now.toISOString().slice(0, 10);
  const [bYear, setBYear] = React.useState(now.getFullYear());
  const [bMonth, setBMonth] = React.useState(now.getMonth());

  // Selection state
  const [selStart, setSelStart] = React.useState(null);
  const [selEnd, setSelEnd] = React.useState(null);
  const [hovDay, setHovDay] = React.useState(null);
  const [selMsg, setSelMsg] = React.useState(null); // { type:'error'|'info', es:'', en:'' }

  React.useEffect(() => {
    fetch('assets/availability.json?t=' + Date.now(), {
      cache: 'no-store'
    }).then(r => r.ok ? r.json() : null).then(j => {
      if (j && j[aptId]) setData(j[aptId]);
    }).catch(() => {}).finally(() => setLoading(false));
  }, [aptId]);
  const blocked = data ? data.blocked : [];
  const updated = data ? data.updated : null;
  const sources = data ? data.sources : [];
  const fetchErrors = data ? data.fetch_errors || {} : {};
  const hasSyncError = Object.keys(fetchErrors).length > 0;
  const isDemo = data ? data.demo : false;

  // Cierre de reservas — última fecha de check-in aceptada (ej. 20 mar 2027).
  // Lee de prices.json (window.PRICES_V2). null = sin restricción.
  const horizonStr = window.PRICES_V2 && window.PRICES_V2.bookingHorizon && window.PRICES_V2.bookingHorizon.lastCheckinDate || null;

  // Estancia mínima: lee de prices.json (window.PRICES_V2.rules.minNights).
  // Default 3 noches si el JSON no llega. Si la entrada elegida tiene menos
  // huecos consecutivos disponibles, ajusta al máximo posible.
  const baseMinNights = window.PRICES_V2 && window.PRICES_V2.rules && window.PRICES_V2.rules.minNights || 3;
  const minNights = React.useMemo(() => {
    if (!selStart) return baseMinNights;
    return Math.min(baseMinNights, _maxConsec(selStart, blocked));
  }, [selStart, blocked, baseMinNights]);

  // Compute preview end for hover display
  let previewEnd = null;
  if (selStart && !selEnd && hovDay && hovDay > selStart) {
    let pathClear = true;
    let cur = _adj(selStart, 1);
    while (cur < hovDay) {
      if (_isBlk(cur, blocked)) {
        pathClear = false;
        break;
      }
      cur = _adj(cur, 1);
    }
    if (pathClear) {
      const pNights = _diff(selStart, hovDay);
      previewEnd = _adj(selStart, Math.max(pNights, minNights));
    }
  }
  const handleDayClick = ds => {
    if (ds < todayStr || horizonStr && ds > horizonStr || _isBlk(ds, blocked)) return;

    // Start new selection: no start yet, or selection complete, or clicked before start
    if (!selStart || selEnd || ds < selStart) {
      setSelStart(ds);
      setSelEnd(null);
      setSelMsg(null);
      setHovDay(null);
      return;
    }
    // Click on start → cancel
    if (ds === selStart) {
      setSelStart(null);
      setSelEnd(null);
      setSelMsg(null);
      setHovDay(null);
      return;
    }

    // ds > selStart, no end yet — check path is clear
    let cur = _adj(selStart, 1);
    while (cur < ds) {
      if (_isBlk(cur, blocked)) {
        setSelMsg({
          type: 'error',
          es: 'Hay días reservados en ese rango. Elige un periodo sin interrupciones.',
          en: 'There are booked days in that range. Please select a period without gaps.'
        });
        return;
      }
      cur = _adj(cur, 1);
    }

    // Enforce minimum nights
    const nights = _diff(selStart, ds);
    if (nights < minNights) {
      const extEnd = _adj(selStart, minNights);
      setSelEnd(extEnd);
      setSelMsg({
        type: 'info',
        es: `Estancia mínima ${minNights} noches — fecha de salida ajustada.`,
        en: `Minimum stay ${minNights} nights — check-out date adjusted.`
      });
    } else {
      setSelEnd(ds);
      setSelMsg(null);
    }
    setHovDay(null);
  };
  const handleReset = () => {
    setSelStart(null);
    setSelEnd(null);
    setSelMsg(null);
    setHovDay(null);
  };
  const m2y = bMonth === 11 ? bYear + 1 : bYear;
  const m2m = bMonth === 11 ? 0 : bMonth + 1;
  const canPrev = !(bYear === now.getFullYear() && bMonth <= now.getMonth());
  const goPrev = () => {
    if (!canPrev) return;
    if (bMonth === 0) {
      setBYear(y => y - 1);
      setBMonth(11);
    } else setBMonth(m => m - 1);
  };
  const goNext = () => {
    if (bMonth === 11) {
      setBYear(y => y + 1);
      setBMonth(0);
    } else setBMonth(m => m + 1);
  };
  const months = lang === 'es' ? _CM.months_es : _CM.months_en;
  const navLbl = `${months[bMonth]} · ${months[m2m]} ${m2y}`;
  const fmtUpd = iso => {
    if (!iso) return '';
    const d = new Date(iso);
    const p = n => String(n).padStart(2, '0');
    return lang === 'es' ? `${d.getDate()} ${months[d.getMonth()].slice(0, 3).toLowerCase()}. · ${p(d.getHours())}:${p(d.getMinutes())}` : `${months[d.getMonth()].slice(0, 3)} ${d.getDate()} · ${p(d.getHours())}:${p(d.getMinutes())}`;
  };
  const sectionStyle = {
    '--apt-accent': accent,
    '--sel-fill': _hexA(accent, 0.22),
    '--sel-circ': _hexA(accent, 0.90),
    '--prev-fill': _hexA(accent, 0.11),
    '--prev-circ': _hexA(accent, 0.48)
  };
  const calProps = {
    blocked,
    lang,
    todayStr,
    horizonStr,
    selStart,
    selEnd,
    previewEnd,
    onDayClick: handleDayClick,
    onDayHover: ds => {
      if (!selEnd) setHovDay(ds);
    },
    onDayLeave: () => setHovDay(null)
  };
  return /*#__PURE__*/React.createElement("section", {
    className: "apt-avail",
    style: sectionStyle
  }, /*#__PURE__*/React.createElement("div", {
    className: "avail-hd"
  }, /*#__PURE__*/React.createElement("div", {
    className: "avail-eyebrow"
  }, /*#__PURE__*/React.createElement("span", {
    className: "avail-dot"
  }), lang === 'es' ? 'Disponibilidad · tiempo real' : 'Availability · live'), /*#__PURE__*/React.createElement("h2", {
    className: "avail-title"
  }, lang === 'es' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("em", null, "Elige tus fechas."), " Tu Hest\xEDa te espera.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("em", null, "Pick your dates."), " Your Hest\xEDa is waiting.")), /*#__PURE__*/React.createElement("p", {
    className: "avail-sub"
  }, lang === 'es' ? 'Calendario sincronizado desde Booking.com y Airbnb. Selecciona entrada y salida para solicitar precio directamente a Alex.' : 'Calendar synced from Booking.com and Airbnb. Select check-in and check-out to request a price directly from Alex.')), isDemo && /*#__PURE__*/React.createElement("div", {
    className: "avail-demo"
  }, lang === 'es' ? '⚡ Datos de ejemplo · La sincronización en tiempo real se activará en breve' : '⚡ Sample data · Live sync will be activated shortly'), !isDemo && hasSyncError && /*#__PURE__*/React.createElement("div", {
    className: "avail-sync-err"
  }, lang === 'es' ? /*#__PURE__*/React.createElement(React.Fragment, null, "\u26A0 La sincronizaci\xF3n autom\xE1tica no pudo conectar. Las fechas mostradas pueden no estar actualizadas. Escr\xEDbenos para confirmar disponibilidad: ", /*#__PURE__*/React.createElement("a", {
    href: "https://wa.me/34620316370",
    target: "_blank",
    rel: "noopener"
  }, "WhatsApp \u2192")) : /*#__PURE__*/React.createElement(React.Fragment, null, "\u26A0 Auto-sync could not connect. Dates shown may not be up to date. Write to us to confirm availability: ", /*#__PURE__*/React.createElement("a", {
    href: "https://wa.me/34620316370",
    target: "_blank",
    rel: "noopener"
  }, "WhatsApp \u2192"))), selStart && !selEnd && /*#__PURE__*/React.createElement("div", {
    className: "avail-hint"
  }, /*#__PURE__*/React.createElement("span", {
    className: "avail-hint-dot"
  }), lang === 'es' ? /*#__PURE__*/React.createElement(React.Fragment, null, "Entrada: ", /*#__PURE__*/React.createElement("strong", null, _shortDate(selStart, 'es')), " \xB7 Ahora selecciona la fecha de salida (m\xEDn. ", minNights, " noches)") : /*#__PURE__*/React.createElement(React.Fragment, null, "Check-in: ", /*#__PURE__*/React.createElement("strong", null, _shortDate(selStart, 'en')), " \xB7 Now select check-out (min. ", minNights, " nights)")), /*#__PURE__*/React.createElement("div", {
    className: "avail-nav"
  }, /*#__PURE__*/React.createElement("button", {
    className: `avail-arr${canPrev ? '' : ' off'}`,
    onClick: goPrev,
    "aria-label": lang === 'es' ? 'Mes anterior' : 'Previous month'
  }, "\u2039"), /*#__PURE__*/React.createElement("span", {
    className: "avail-nav-lbl"
  }, navLbl), /*#__PURE__*/React.createElement("button", {
    className: "avail-arr",
    onClick: goNext,
    "aria-label": lang === 'es' ? 'Mes siguiente' : 'Next month'
  }, "\u203A")), loading ? /*#__PURE__*/React.createElement("div", {
    className: "avail-months"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cal-month cal-skel"
  }), /*#__PURE__*/React.createElement("div", {
    className: "cal-month cal-skel"
  })) : /*#__PURE__*/React.createElement("div", {
    className: "avail-months"
  }, /*#__PURE__*/React.createElement(CalMonth, _extends({
    year: bYear,
    month: bMonth
  }, calProps)), /*#__PURE__*/React.createElement(CalMonth, _extends({
    year: m2y,
    month: m2m
  }, calProps))), selMsg && /*#__PURE__*/React.createElement("div", {
    className: `avail-sel-msg ${selMsg.type}`
  }, selMsg.type === 'error' ? /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "12",
    y1: "8",
    x2: "12",
    y2: "12"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "12",
    y1: "16",
    x2: "12.01",
    y2: "16"
  })) : /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), /*#__PURE__*/React.createElement("polyline", {
    points: "9 12 11 14 15 10"
  })), lang === 'es' ? selMsg.es : selMsg.en), selStart && selEnd ? /*#__PURE__*/React.createElement(RequestPanel, {
    aptId: aptId,
    lang: lang,
    accent: accent,
    selStart: selStart,
    selEnd: selEnd,
    onReset: handleReset
  }) :
  /*#__PURE__*/
  /* Footer — only shown when no complete selection */
  React.createElement("div", {
    className: "avail-foot"
  }, /*#__PURE__*/React.createElement("div", {
    className: "avail-legend"
  }, /*#__PURE__*/React.createElement("span", {
    className: "leg-item"
  }, /*#__PURE__*/React.createElement("span", {
    className: "leg-dot lg-av"
  }), lang === 'es' ? 'Disponible' : 'Available'), /*#__PURE__*/React.createElement("span", {
    className: "leg-sep"
  }, "\xB7"), /*#__PURE__*/React.createElement("span", {
    className: "leg-item"
  }, /*#__PURE__*/React.createElement("span", {
    className: "leg-dot lg-bk"
  }), lang === 'es' ? 'Reservado' : 'Booked'), selStart && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "leg-sep"
  }, "\xB7"), /*#__PURE__*/React.createElement("span", {
    className: "leg-item"
  }, /*#__PURE__*/React.createElement("span", {
    className: "leg-dot lg-sel"
  }), lang === 'es' ? 'Tu selección' : 'Your selection'))), /*#__PURE__*/React.createElement("div", {
    className: "avail-meta"
  }, updated && /*#__PURE__*/React.createElement("span", {
    className: "avail-upd"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "11",
    height: "11",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.5",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), /*#__PURE__*/React.createElement("polyline", {
    points: "12 6 12 12 16 14"
  })), fmtUpd(updated)), sources.length > 0 && /*#__PURE__*/React.createElement("span", {
    className: "avail-srcs"
  }, sources.includes('booking') && /*#__PURE__*/React.createElement("span", {
    className: "avail-src src-bk"
  }, "Booking.com"), sources.includes('airbnb') && /*#__PURE__*/React.createElement("span", {
    className: "avail-src src-ab"
  }, "Airbnb")))));
};
Object.assign(window, {
  AptCalendar,
  CalMonth,
  RequestPanel
});
