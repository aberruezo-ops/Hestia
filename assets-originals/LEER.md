# Originales sin optimizar

Copias previas a la optimización de las fotos de galería de Hestía Thalassa.
Cada una tiene su versión servida en `docs/assets/` (JPG optimizado + WebP).

Viven fuera de `docs/` a propósito: dentro se publicaban en GitHub Pages, se
desplegaban en cada build y quedaban rastreables, sin que ninguna página las
usara. Aquí siguen versionadas y disponibles para reprocesar, sin publicarse.
